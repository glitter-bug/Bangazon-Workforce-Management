@model Bangazon_Workforce_Management.Models.ViewModels.TrainingAssignViewModel

@{
    ViewData["Title"] = "Assign";
}

<h2>Assign</h2>

<h4>EmployeeTraining</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Assign">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            
            <div class="form-group">
                <label asp-for="EmployeeId" class="control-label"></label>
                <label asp-for="EmployeeId" class="form-control" />
                <span asp-validation-for="EmployeeId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="TrainingPrograms" class="control-label"></label>
                <select asp-for="TrainingProgramId" asp-items="@Model.TrainingPrograms"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
